{% extends 'base.html.twig' %}

{% block title %}New Choice{% endblock %}

{% block body %}
    <h1>Create new Week</h1>

   {{form(form)}}

    <a href="{{ path('app_week_index') }}">back to list</a>

    {{react_component('handleFormChanges')}}
    
{% endblock %}
